# Discord
DISCORD_TOKEN=
DISCORD_GUILD_ID=
DISCORD_CHANNEL_ID=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# App
BASE_URL=https://www.emarketplace.state.pa.us/Procurement.aspx
CHECK_INTERVAL_MINUTES=60
USER_AGENT=
RESET_STATE_ON_START=false

# VPN (optional)
# Path to .ovpn inside the container (default points to bundled Windscribe sample)
OVPN_CONFIG=/app/data/Windscribe-Atlanta-Mountain.ovpn
# If your .ovpn requires user/pass, set both of these (container-only)
OVPN_AUTH_USERNAME=
OVPN_AUTH_PASSWORD=
